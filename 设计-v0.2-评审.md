# dsh-escrow v0.2 设计评审

> 评审对象：`设计-v0.2.md`（v0.2.0 设计稿）
> 评审结论：**不通过，需修订后再开工。** 发现 2 个机制性错误、4 个安全组合漏洞、4 个中危问题、5 项建议新增。

---

## 一、机制性错误（必须重写方案）

### P1【致命】M1 的 deferContext 注入方案在时间上不成立

**问题**：设计假设"批准后执行，真实结果通过 `deferContext()` 在下一轮反馈给模型"。但查 dsh 的 API 契约：`deferContext` 的语义是"**延迟注入直到该工具的最终结果到达 agent loop**"——它绑定在**本次执行的生命周期内**。而 M1 的合成结果本身就是"最终结果"，defer 窗口在返回合成结果的瞬间就关闭了。批准发生在 30 秒后，**那时 deferContext 早就失效**。跨轮注入走不通。

**修正方案（三选一，建议 A）**：
- **方案 A（推荐）：面向模型的轮询工具。** 注册一个模型可见工具 `escrow_result(id)`。合成结果明确写"动作已托管 esc-xxx，未执行；可调用 escrow_result 查询结果"。模型可以在后续步骤主动查询（等它需要的动作出结果），也可以先做别的。这是把 Pydantic AI 的 deferred 模式改造成"轮询式"，全部走 dsh 原生工具注册机制，零魔法。
- 方案 B：批准后把执行结果作为 user-visible 消息注入下一轮（需 spike dsh 的会话消息注入 API，approval 服务的 notify_agent 有类似机制可参考）。
- 方案 C：放弃非阻塞，M1 降级为"仅缩短感知延迟"（synthetic result 换成同步等待 + UI 侧显示）——保底方案。

### P2【致命】重放执行的死循环：escrow 会拦下自己批准的动作

**问题**：批准后"执行该动作"意味着重新走一遍执行管线——**重新经过 `tools/pre-execute` 瀑布 → 分类仍是 red → 再次入队 → 死循环**（或至少重复打扰）。设计完全没处理。

**修正方案**：引入**重放豁免令牌（replay token）**。批准时生成一次性令牌绑定 (id, exec快照hash)；重放执行携带令牌，escrow 的 pre-execute 监听器识别到有效令牌 → 直接放行并记账 `escrow.approved_executed`（含令牌验证记录）。令牌单次、有过期、验证失败按普通 red 处理。

---

## 二、安全组合漏洞（攻击者可直接利用）

### P3【高危】escrow 自身资产不在敏感路径内——白名单可被 agent 篡改

**问题**：内置敏感路径覆盖了 `.env`、`.ssh` 等，但**不包含 `$DSH_HOME/.dsh-escrow/` 自己**。agent 一条 `bash: echo '{}' > ~/.dsh/escrow/allowlist.json` 就能清空黑名单、伪造白名单——**防线本身可以被绕过**。这是安全工具的经典自杀漏洞。

**修正**：
1. 内置敏感路径**必须**包含 `.dsh-escrow/`（账本、白名单、黑名单、密钥）→ 写入即 red
2. 账本采用 append-only 文件 + 启动时完整性校验（行数/末行哈希比对，被截断则告警并从轮转文件恢复）
3. 白名单/黑名单写入前校验 schema，格式异常拒绝加载并告警（防半篡改）

### P4【高危】自改动作 × 学习白名单 = 孔某人失败模式的复刻

**问题**：M7 把"写 AGENTS.md / 记忆文件"设为 red，但 M2 的学习白名单没有排除自改动作。后果：agent 改自己被拦两次、你批准两次 → 进入白名单 → **从此随意改自己**。这正是孔某人文章的失败路径："我只是允许它处理小问题"，然后失控。

**修正**：**自改类签名永不进入学习白名单**（只能通过 `/escrow allow <sig>` 手动、显式、带确认提示地加入）。同样原则适用于其他"不可学习"类别（见 P7）。

### P5【高危】M5 的 Telegram 审批与 loopback 服务自相矛盾

**问题**：审批链接指向 `127.0.0.1:8787`，但 Telegram 消息是在**手机浏览器**里点开的——127.0.0.1 在手机上没有意义。要么链接公网可达（令牌经过 Telegram 服务器、可能被日志记录/转发 = 凭据出本机），要么审批根本点不动。设计没发现这个矛盾。

**修正**：**通知与审批分离**（Agent Inbox 模式而非"链接即能力"模式）：
- Telegram/webhook 只做**通知**（"有 esc-xxx 待审"）
- 审批通过 **bot 消息回传**：你回复 `/approve esc-xxx` 给 bot，本机插件通过 Telegram Bot API（出站长轮询 getUpdates）拉取决策，令牌**永不出本机**
- loopback HTTP 服务保留给"你人在电脑前"的场景

### P6【高危】审批通道绝不能暴露为模型可调用工具

**问题**：设计没有写死这条。未来任何一次重构，如果 `/escrow approve` 被注册成模型可见工具（哪怕只是"为了方便"），模型就能**自己批准自己**——整个系统在那一刻归零。

**修正**：写进"宪法条款"（见新增 N1）：审批命令只走 human command 通道（`ctx.commands.register` 的 handler），**永远不注册为模型可见工具**，且 handler 内校验调用源为人类 UI 事件。这条要加注释、加测试（防止未来回归）。

---

## 三、中危问题

### P7 毒化与签名漂移：学习白名单需要"不可学习名单"

- **毒化**：提示注入可以在你疲劳时诱导误批准 → 危险签名进白名单。
- **漂移**：`git push origin <BRANCH>` 白名单化后，`git push origin --force` 的 `--force` 可能被规范化当成分支名吞掉。
- **修正**：①规范化规则改为**白名单式替换**——只有匹配已知安全形态（分支名、数字、哈希）的 token 才替换为通配符，`--` 开头的 flag 一律保留原文；②内置**不可学习名单**（never-learn list）：`rm -rf` 类、任何带 `--force`/`-f` 的命令、自改类、`dd`/`mkfs`/磁盘类——这些签名无论批准多少次都不自动放行；③白名单条目加 24 小时冷却期（批准后不立即生效）。

### P8 可补偿性应与拦截严格度耦合

设计里补偿（M4）和拦截（分类）是两个独立旋钮。正确耦合：**无法生成补偿动作的红灯，应该更难放行**（不能救的就更该拦）——`timeoutPolicy` 对"不可补偿"类动作强制 `cancel`，`release` 策略对它们无效。

### P9 非阻塞需要 per-tool 逃生门

模型可能忽略"已托管未执行"的提示、继续做依赖该结果的后续动作（读刚"写"完的文件）。修正：配置项 `syncTools: []` 允许把"后续强依赖结果"的工具（典型：写后读）留在同步模式；报告中统计"托管期间误依赖"次数（合成结果中嵌入一次性标记，后续调用引用了相关路径则计数）。

### P10 账本与队列缺会话上下文

v0.1 账本不记 sessionId/turnId，审计时无法回溯"哪个会话哪个回合"。修正：ledger 每条记录补 `session`（从 `exec.agent` 派生）与 `turn` 字段；pending 队列按会话隔离展示（`/escrow review` 默认当前会话，`--all` 看全部）。

---

## 四、建议新增

| # | 建议 | 内容 | 归属 |
|---|---|---|---|
| **N1** | **宪法条款（Constitution）** | 一组不可通过配置关闭的硬规则：①审批通道永不暴露给模型（P6）②`.dsh-escrow/` 自身写入恒为 red（P3）③自改签名永不学习（P4）④超时对不可补偿动作恒为 cancel（P8）⑤fail-closed 恒开。写在代码常量里，文档显著标注，测试覆盖 | 新增顶层概念 |
| **N2** | **/escrow doctor 自检** | 检查自身完整性：账本末行哈希、白名单 schema、密钥文件权限、插件树 hash 变化、规则数上限。自检不过输出告警。**安全工具必须自身可验证** | 新命令 |
| **N3** | **规则包格式** | 白名单+规则导出为带版本号与校验和的 YAML 包（`escrow-rules.yaml`），团队可共享、可审计、可签名——这就是"配方"资产的落地形态 | M2 扩展 |
| **N4** | **性能预算** | 分类器在每次工具调用的热路径上：规则数上限（如 256）、正则预编译缓存、预算指标（pre-execute 附加延迟 < 1ms，超预算告警）。安全工具自己不能成为性能税 | 全局约束 |
| **N5** | **误依赖观测** | 记录"托管期间模型仍引用未执行动作结果"的次数（P9 的观测面）——这是非阻塞设计的健康指标，也是将来写论文/报告的素材 | M1 配套 |

---

## 五、修订后的 M1 方案（替换原稿）

```
pre-execute: 分类（不变）
  ├─ green/yellow：照旧
  └─ red：
       ├─ 白名单命中 → 放行（记 escrow.whitelisted）
       ├─ 入队（记 escrow.queued）+ 外部通知（M5，仅通知）
       └─ tools/execute 包装：
            不调 next()，返回合成结果：
            「动作 esc-xxx 已进入托管队列，尚未执行。
             需要结果时调用 escrow_result(esc-xxx) 查询；
             不依赖此结果的工作可继续。」
       ┌─ 批准（human command / bot 回传）：
       │    生成一次性 replay token → 重放执行（带令牌，pre-execute 豁免）
       │    → 真实结果存入队列记录 → 模型下次 escrow_result 查询时返回
       ├─ 拒绝/超时：记 escrow.denied_executed（模型查询时返回拒绝原因）
       └─ 新增模型可见只读工具：escrow_result(id) —— 查询托管结果
```

**同步降级**：`mode: sync` 保留 v0.1 行为；`syncTools: []` 提供工具级逃生门。

**S0 Spike 清单（修订）**：
1. `tools/execute` 返回合成结果后，agent loop 是否重复调用同工具（重试风暴？）
2. `escrow_result` 工具注册为模型可见的正确姿势（含 Code Mode 下的投影）
3. 重放执行的豁免令牌在 pre-execute 的验证路径
4. （删除原 deferContext spike——方案已替换）

---

## 六、评审后的里程碑变化

| 阶段 | 变化 |
|---|---|
| S0 Spike | 3 项验证（合成结果行为 / escrow_result 注册 / 豁免令牌），2–3 天不变 |
| S1 | M1 重写（轮询式）+ M2 学习白名单（含宪法条款、不可学习名单、冷却期）+ **P3 自身保护**——约 2.5 周 |
| S2 | M3 批量审阅 + M6 账本治理（补 session 上下文）+ N2 doctor + N5 误依赖观测 |
| S3 | M4 命令层补偿（含 P8 耦合规则） |
| S4 | M5 重写为"通知/审批分离"（bot 回传，令牌不出本机） |
| S5 | M7 自改治理（永不学习）+ N3 规则包 + N4 性能预算 |

**总工期不变（5–6 周），但 S1 增加了宪法条款与自身保护——安全工具先保护好自己，再保护别人。**
