# 说明书改写方案：从「安全层」到「Reduced by」

> 依据：Rick Rubin《Reduced by》视角（见当日讨论）+ v0.2 设计细则（已评审修订）
> 本方案回答两件事：①下一版新增什么功能 ②说明书怎么改写

---

## 一、新增功能（2 个，含 1 个合并）

### M9 减法审计（Subtraction Audit）——Rubin 减法测试的产品化

**定位**：escrow 不只拦"危险的"，还指出"多余的"。守卫升格为编辑者。

**命令**：`/escrow reduce [--since 7d]`

**分析维度（v0.2 全确定性规则，无 AI）**：
| 维度 | 检测逻辑 | 输出 |
|---|---|---|
| **重复动作** | 同一签名（M2 的签名机制）执行 ≥ N 次（默认 10） | "该模式重复 47 次，建议缓存/合并" |
| **零变更写入** | 写操作但内容前后一致（依赖 M4 的 before/after 对象） | "37 次写入未产生任何变化，疑似空转" |
| **冷工具** | 整个账本周期内某工具贡献的有效动作数为 0 | "工具 `web.search` 零贡献，考虑关闭" |
| **冷插件** | 按插件树 hash 关联：某插件在册期间贡献动作数为 0 | "插件 `xxx` 疑似零贡献，建议卸载" |

**输出**：减法清单（每条附账本证据行号，可核查）+ 预估收益（省 token / 省上下文 / 省注意力）。

**边界（宪法级）**：escrow **永远不自动卸载**——编辑者只提建议，创作者拍板。建议条目一键生成对应的用户规则（如"关闭某插件"仍需人工执行）。

**Rubin 对应**：减法测试——"逐个拿掉，直到作品即将失去意义"。这里反过来：逐个标记，直到清单上剩下的都是 agent 真正需要的。

### M10 信噪比与署名（SNR & Signature）——让"Reduced by"可见

**指标**：
```
SNR = 有效动作 ÷ 总动作
辅助：重复率 / 零变更率 / 打扰率（确认次数 ÷ 动作总数）
```
- v0.2 的"有效"是**近似定义**（诚实）：总动作 − 重复动作 − 零变更动作；完整的输出依赖追踪（动作 A 的结果被 B 引用）放 v0.3。
- **署名**：月度报告自动生成固定格式结尾——
  ```
  ── Reduced by dsh-escrow ──
  本月：拦下 12 个危险动作 · 静默 3,206 次 · 建议减去 2 个冷插件 · SNR 0.41 → 0.58
  ```
- 这是公开轨迹的产物：可分享、可对比（SNR 随时间的趋势 = agent 卫生状况的体检曲线）。

### 合并决定：M8 并入 M10（对自家路线图做减法）

v0.2 原有 M8"成本显示"与 M10 的署名/报告同属"让价值可见"——**用编辑者身份审自己的路线图：M8 独立成模块是冗余，合并**。v0.2 仍是 8 个模块（M1-M7 + M9/M10 合并计），工期不变。

---

## 二、说明书改写方案（章节级 diff）

| 旧章节 | 处置 | 新章节 |
|---|---|---|
| 一、它是做什么的 | **重写** | 一、定位：agent 的编辑者（新定位语见下） |
| 二、它解决什么问题 | 保留微调 | 二、背景（不变，补一句"生产过剩、品味稀缺"） |
| 三、v0.1 功能清单 | 保留 | 三、功能（重组为四幕结构，见下） |
| 四、工作原理 | 保留 | 四、原理（不变） |
| 五、安装 | 保留 | 五、安装（不变） |
| 六、使用示例 | 保留 | 六、使用（不变） |
| 七、配置参考 | 更新 | 七、配置（补 v0.2 字段） |
| 八、测试 | 保留 | 八、测试（不变） |
| 九、已知限制 | 保留 | 九、限制（不变） |
| 十、升级路线 | **重写** | 十、路线图（并入 M9/M10、品味订阅） |
| —（无） | **新增** | **十一、设计哲学：Reduced by**（新写，全文见下） |
| —（无） | **新增** | **十二、术语表**（品味档案 / 显式品味 / 品味习得 / 信噪比） |

### 新定位语（替换原"一句话"）

> 旧：「给 DeepSeek Harness 补上生效前托管这一层：危险动作先托管，批准才执行。」
> **新：「dsh-escrow 是 agent 的编辑者——它不帮 agent 多做任何事，只决定什么不该发生、什么不值得留下。拦下危险的，删去多余的，记住你的品味。」**

### 新增章节《十一、设计哲学：Reduced by》全文（直接可用）

---

**传奇制作人 Rick Rubin 的专辑署名不是 Produced by，而是 Reduced by。** 他不会乐器、不懂调音台，但顶级音乐家愿意付大钱找他——因为他卖的不是制作，是取舍：帮你找回被专业工艺掩盖的、最有力量的部分。

**Agent 时代正在重演同一个故事，只是方向相反。**

模型越来越强、工具越来越多、插件越来越密——**加法的世界里，产能正在无限供给**。一万一千个插件、九百多个真的有人用；一个自演化 agent 一天把自己改到臃肿失控。生产过剩的另一半，永远是：**没有人负责减法。**

dsh-escrow 站的位置就是减法：

- **拦（托管）**：不该发生的动作，生效前拦住——这是安全；
- **学（品味）**：你批准过什么、拒绝过什么，系统记成你的品味档案——同一个模式只打扰你一次；
- **减（审计）**：哪些动作是空转、哪些工具零贡献、哪些插件该卸载——它给出建议，你来拍板。

我们用两个数字描述 agent 行为的卫生状况：**信噪比**（有效动作 ÷ 总动作）和**打扰率**（确认次数 ÷ 动作总数）。好的编辑者让前者上升、后者下降——**工具用它自己的账本证明它值得你的注意力。**

这个哲学有一条来自 Rick Rubin 的边界，也是本插件的宪法之一：**编辑者永远不替创作者拍板。** escrow 给出减法建议，但从不自动卸载；它拦下危险动作，但放行的权力永远在人手里。

**当所有人都在给 agent 做加法，做减法的人就成了稀缺品。**

---

### 十二、术语表（新增，节选）

| 术语 | 技术名 | 含义 |
|---|---|---|
| 品味档案 | allowlist / denylist | 你的批准与拒绝沉淀成的自动放行/拒绝规则 |
| 显式品味 | rules（用户规则） | 你亲手写的规则，优先级最高 |
| 品味习得 | learning whitelist | 同一模式批准 N 次后自动放行的机制 |
| 减法审计 | subtraction audit | 找出零贡献动作/工具/插件的建议清单 |
| 信噪比 | SNR | 有效动作 ÷ 总动作 |
| 署名 | Reduced by signature | 月度报告的固定结尾，公开轨迹的产物 |

---

## 三、路线图更新（说明书第十节替换）

v0.2（在评审修订版基础上）：M1-M7 不变 + **M9 减法审计（简化版：重复/零变更/冷工具冷插件统计）并入 M6 报告**。
v0.3：完整依赖追踪（真 SNR）+ **品味订阅（规则包的 Rubin 版：订阅某人调校过的品味档案）** + 跨层事务 + 取证。
v1.0：认证插件集 + 标准草案 + 减法审计企业版（政企的"agent 卫生检查"）。

---

## 四、工期影响

M9 简化版约 3-4 天（纯统计，复用 M2 签名与 M4 对象）；M10 署名约 1-2 天（M6 报告的格式化输出）。**通过 M8 并入省回 3 天——总工期仍 5-6 周，净增约 1-2 天。**
