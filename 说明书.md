# dsh-escrow v0.1.1 使用说明书

> 版本：v0.1.1（2026-08-28）｜ 许可证：MIT ｜ 位置：`D:\BigWorkspace\projects\dsh-escrow`
>
> v0.1.1 变更：修复 v0.1 审核发现的 14 个问题（4 个分类器绕过类高危、`ttlSec=0` 失效、
> 绿灯规则压制内置红灯、审批者看不到动作内容、JSON 形态密钥明文入账、`args.key` 被忽略、
> 幽灵待决条目、next() 可能双调用、catch 放行已判红动作、测试挂起等），全部配攻击样本回归测试。

---

## 一、它是做什么的（一句话）

**dsh-escrow 给 DeepSeek Harness 补上「生效前托管」这一层：agent 的不可逆动作先进入托管队列，人工批准才执行，超时默认拒绝（silence means no）。**

它解决的不是"出事之后怎么回滚"，而是"出事之前怎么拦住"。

## 二、它解决什么问题（背景）

| 现状 | 问题 |
|---|---|
| dsh 三档权限（read-only / workspace-write / danger-full-access） | 实测管不住插件——插件就是 dsh 本身，"保安不查同事" |
| 官方 approval 服务的 `ask` 档 | 无头环境下没人应答会把整个运行卡死（SSDNodes 实测指出的死穴） |
| 现有回滚插件（dsh-undo-savepoint、dsh-rollback） | 都是**事后**撤销：快照、文件回滚。没有一个是**生效前**拦截 |
| 你的机器实况（web profile） | `danger-full-access` + `approval: never`——当前所有动作无检查直接放行 |

dsh-escrow 补的是**第四态**：不是 allow、不是 deny、不是 ask（阻塞），而是**托管**——动作进队列，给一个可取消的窗口，窗口内批准才放行，超时默认拒绝。

## 三、v0.1 当前功能清单

### 3.1 动作分类引擎（classify.mjs）

每次工具调用在 `tools/pre-execute` 钩子上被分类：

| 档位 | 含义 | 处置 |
|---|---|---|
| 🟢 green | 只读/安全 | 直接放行，不记账 |
| 🟡 yellow | 有副作用但在工作区 | 放行 + 写入账本（observe） |
| 🔴 red | 不可逆/危险 | **进入托管队列**，批准才执行 |

**内置规则（builtinRules，可关闭）**：
- 危险命令正则（v0.1.1 起全部大小写不敏感，覆盖分离/长旗标与 `git -C`、`git.exe` 等变体）：`rm -rf`（含 `-r -f`、`--recursive --force`）、`git push`、`git clean -fdx`、`git reset --hard`、`git checkout -- .`、PowerShell `Remove-Item -Recurse`、`del/rd/rmdir /s`、`mkfs`、`format`、`shutdown`、`reboot`、`dd if=`、`curl|sh`、`wget|sh`、fork 炸弹、`chmod -R 777 /`、`chown -R /` 等
- **命令串涉敏扫描**（v0.1.1 新增）：shell 命令文本中出现 `.env`、`.ssh/`、`id_rsa` 等私钥/凭据字样即 red——`cat ~/.ssh/id_rsa` 这类不经 path 参数的访问不再漏网
- 敏感路径 glob（大小写不敏感，递归读取嵌套参数与 `filePath` 等键名变体）：`**/.env*`、`**/.ssh/**`、`**/id_rsa*`、`**/*.pem`、`**/credentials*`、`**/secrets/**`、`**/*.key`、`**/auth.json`、`**/.npmrc`、`**/.aws/**`、`**/.gnupg/**`、`**/.kube/**`、`**/*.token` 等

**优先级（v0.1.1 修正）**：
1. 用户规则 first-match-wins，规则内约束为 AND（tools 命中 && args 各模式按各自 key 命中 && paths 命中）；**但用户 green 不能压制内置 red**（防 `^ls ` 前缀规则放行 `ls -la; rm -rf x`），用户 yellow/red 可以覆盖内置结果；
2. 内置规则命中 → red；
3. 用户 green 在内置未命中时生效；
4. 皆未命中 → `defaultAction`（默认 yellow，可配）。

**设计原则**：
- 确定性规则，绝不用 AI 分类器做安全决策（避免"默认同意"式漏洞）
- 分类器自身异常 → 不阻断主链路（fail-open to next）；但**已判 red 后链路异常 → fail-closed 拒绝**，绝不把 red 静默降级

### 3.2 托管队列（queue.mjs）

| 配置项 | 默认 | 说明 |
|---|---|---|
| `ttlSec` | 30 | 托管窗口（秒）。**0 = 立即拒绝**（CI / 无头 fail-closed 姿态；v0.1.1 修复了 0 被静默改成 30 的 bug） |
| `timeoutPolicy` | cancel | 超时处置：`cancel`（默认拒绝）｜ `release`（放行，需显式配置）｜ `hold`（无限等待） |
| 信号中止 | — | agent turn 被取消 → `cancelled`，按拒绝处理 |

决策结果带 `via` 字段区分来源（`human` 人工 / `timeout` 超时 / `immediate` ttl=0 立即 / `abort` 信号中止），
模型收到的拒绝理由会区分「已被人工拒绝」与「未在窗口内获批」。
立即拒绝/预中止的条目不会残留在待决列表（v0.1.1 修复幽灵条目）。

超时语义（Agent Inbox 原则）：**无人批准 = 拒绝，绝不默认放行。**

### 3.3 append-only 账本（ledger.mjs）

- 路径：`$DSH_HOME/.dsh-escrow/ledger.jsonl`（默认 `~/.dsh/.dsh-escrow/`）
- 只追加，不修改不删除；每行一条 JSON 记录
- 记录种类：`observe`（黄灯）/ `escrow.queued`（红灯入队）/ `escrow.decided`（决策结果，含 via）
- **密钥自动脱敏**（v0.1.1 修复 JSON 形态漏脱敏）：sk-、Bearer、AIza、AKIA、ghp_、xox、
  api_key/password/secret/token/authorization（键名带不带引号都匹配）、PEM 私钥块 → `***REDACTED***`
- **轮转**（v0.1.1 新增）：超过 `ledgerMaxMb`（默认 32MB）轮转为 `ledger.jsonl.bak`（只保留一代）；
  启动行数统计改为分块读取，不再整文件入内存
- 账本写失败不阻断主链路（记录 stderr，agent 继续）

### 3.4 /escrow 命令（commands.mjs）

| 命令 | 作用 |
|---|---|
| `/escrow pending` | 列出待决托管动作（id、工具、等待时长、理由、**脱敏后的动作摘要**——批准的正是你看到的那条） |
| `/escrow approve <id>` | 批准 → 放行执行（回显动作摘要） |
| `/escrow deny <id>` | 拒绝 |
| `/escrow stats` | 策略状态 + 待决数 + 账本行数与路径 |
| `/escrow help` | 帮助 |

## 四、工作原理

```
工具调用
  │
  ▼ tools/pre-execute（waterfall，不碰 dsh 源码）
  ├─🟢 放行（next()）
  ├─🟡 放行 + 写账本 observe
  └─🔴 入队（记 escrow.queued）→ 等待窗口
        ├─ /escrow approve <id> → 放行执行
        ├─ /escrow deny <id>   → 拒绝（模型收到 deny 与理由）
        ├─ 超时 → 按 timeoutPolicy（cancel 默认拒绝）
        └─ turn 取消 → cancelled（按拒绝）
```

- 纯插件（bundle），挂在 Cordis 事件接缝上，**不改任何 dsh 源码**
- **不依赖官方 approval 服务**（policy: never 时它永远拒绝且无内置 TTL）——自包含队列
- 插件卸载时 Cordis 自动回收监听与命令注册（可逆副作用）

## 五、安装

### 5.1 前置

- dsh CLI 可用；pnpm 在 PATH（本机：`D:\home\flora\.local\pnpm`）
- **Windows 注意**：`dsh plugin add <本地目录>` 的 `link:` 方式会建成空目录（实测踩坑），**必须用 tarball**；pnpm 请走 PowerShell 通道（Bash 里 pnpm 会被安全策略拦截）

### 5.2 步骤（PowerShell）

```powershell
# 1. 打包（在项目目录）
Set-Location D:\BigWorkspace\projects\dsh-escrow
pnpm pack                      # 产出 dsh-escrow-0.1.0.tgz

# 2. 安装进 profile（web / headless / 自建）
$env:PATH = "D:\home\flora\.local\pnpm;" + $env:PATH
node D:\home\flora\.local\node_modules\@deepseek-ai\dsh\lib\bin.js plugin --profile web add "./dsh-escrow-0.1.0.tgz"

# 3. 验证插件树（应看到 # == dsh-escrow → id: escrow）
node D:\home\flora\.local\node_modules\@deepseek-ai\dsh\lib\bin.js --profile web --dump-config | Select-String -A6 "escrow"

# 4. 重启 dsh（改 profile 的 package.json 后必须重新 pnpm install，否则启动报
#    cannot resolve profile bundle；安装命令已自动处理）
```

## 六、使用示例

**场景：agent 试图执行 `rm -rf`（红灯）**

1. agent 发起调用 → escrow 拦截入队，返回 deny 提示 `[escrow:esc-123-1] 未在窗口内获批，默认拒绝 (内置危险命令模式)` —— 或者你赶在窗口内处理：
2. 输入 `/escrow pending`：
   ```
   [escrow] 1 个待决动作：
     esc-123-1  bash  (12.3s)  危险命令模式: \brm\b...
         ↳ {"command":"rm -rf /tmp/x"}
   批准: /escrow approve <id>
   拒绝: /escrow deny <id>
   ```
3. 确认没问题 → `/escrow approve esc-123-1` → 放行执行
4. 或 `/escrow deny esc-123-1` → 拒绝
5. 任何决策都写入账本，可事后审计

**场景：CI / 无头环境**：`ttlSec: 0` → 所有红灯动作立即拒绝（fail-closed），白名单靠用户规则显式声明。

## 七、配置参考（完整字段）

```yaml
# 安装后改配置：编辑 profile 的 cordis.patch.yml 中 escrow 行的 config
- id: escrow
  name: dsh-escrow
  config:
    ttlSec: 30              # 窗口秒数；0 = 立即拒绝
    timeoutPolicy: cancel   # cancel | release | hold
    defaultAction: yellow   # green | yellow | red（规则未命中时）
    builtinRules: true      # 内置危险命令/敏感路径规则开关
    ledgerDir: ""           # 账本目录，默认 $DSH_HOME/.dsh-escrow
    rules: []               # 用户规则（first match wins，规则内 AND）
      # 示例：
      # - id: block-git-push
      #   tools: ['bash', 'pwsh']
      #   args: [{ key: command, pattern: 'git push' }]
      #   action: red
      # - id: allow-safe-tests
      #   tools: ['bash']
      #   args: [{ key: command, pattern: '^(pytest|npm test) ' }]
      #   action: green
```

⚠️ dsh 的 patch 是**整行替换**，不深合并——改一个字段也要重述全部字段。

## 八、测试

```bash
# 单元测试（classify / queue / ledger，不依赖 dsh 运行时，无需安装）
node test/smoke.test.mjs                    # 43 个断言（含 19 条攻击样本/bug 回归）

# 插件级集成测试（mock Cordis ctx，全链路含批准放行；需在装好插件的 profile 内运行）
cd C:/Users/flora/.dsh/profiles/escrowtest
node --input-type=module D:/BigWorkspace/projects/dsh-escrow/test/plugin.integration.test.mjs   # 14 个断言
```

实测结果（v0.1.1）：43/43 单元 + 14/14 集成全部通过，套件 0.4s 内退出（v0.1 会挂 60 秒）。
集成测试含时延断言（ttl=0 实测 4ms 返回）与 next() 单调断言（下游同步抛错时不得二次执行）。

## 九、已知限制（诚实清单，v0.1.1）

| 限制 | 说明 | 计划 |
|---|---|---|
| 同步窗口 | 红灯动作等待期间 agent 暂停（红灯本来就该停下来） | v0.2 非阻塞 |
| 无插件来源守卫 | 无法识别动作发起方是模型还是某个插件（v0.1 API 无此信息） | v0.3 |
| 无命令层补偿/撤销 | 只拦截不撤销；撤销属于 dsh-rollback 的领域 | v0.2 配合 |
| 无 UI 通知 | 只有 /escrow 命令，无弹窗/外部通知 | v0.2 |
| 规则基于正则/glob | 非语义理解； Windows 语义下大小写已不敏感，但混淆编码、间接引用（变量/脚本文件）等仍可能绕过——它是减速带不是密码学边界 | 持续演进 |
| 账本只保留一代轮转 | 超过 `ledgerMaxMb` 后轮转，历史只有 `.bak` 一代；长期归档需自行备份 | v0.2 报告导出 |
| 无跨层事务一致性 | 配置/文件/命令三层各自独立 | v0.3 |
| 端到端已在真实 dsh 部分验证 | v0.1.1 于 escrowtest profile 实测通过（2026-08-29）：黄灯放行记账、红灯入队、`ttl=0` 立即拒绝（waitedMs=0）、提权绕过尝试仍被拦、拒绝理由被模型正确理解；**S0 三 spike（合成结果重试/escrow_result 注册/重放令牌路径）与 TUI 内 /escrow 命令仍待验证** | W0 剩余项 |

## 十、升级路线图

### v0.2（1–2 周）：补全托管体验
1. **非阻塞托管**：在 `tools/execute` 层包装，红灯动作返回"已托管"伪结果，agent 继续跑别的分支；批准后重放执行（技术风险点：dsh 的执行重放机制，需实测）
2. **命令层补偿**：记录 shell 命令并生成反向命令（与 dsh-rollback 的文件层配合，补齐"命令副作用可回滚"）
3. **外部通知**：webhook / Telegram / 邮件审批通道（借鉴 Vultrino 模式），无头环境也能及时审批
4. **账本治理**：保留策略、`/escrow report` 导出审计报告（JSON + Markdown）（轮转已在 v0.1.1 落地）
5. **成本显示**：每 turn 打印 token / 缓存影响估算（装插件会让 KV 缓存作废，这个数字值得让用户看见）

### v0.3：事务化与取证
1. **插件来源守卫**：识别动作发起方插件 id，非内置来源默认升级风险等级（需要 dsh 上层 API 配合）
2. **检查点与跨层一致性**：一次 turn = 一个事务单元，配置层（dsh-undo-savepoint）+ 文件层（dsh-rollback）+ 命令层（本插件）的组合回滚
3. **取证报告**：一次会话导出完整审计链（检查点、红灯清单、决策记录、插件树 hash），供保险/监管/第三方验证读取
4. **规则集市**：共享规则包，社区可订阅（安全基线即代码）

### v1.0：接入生态、成为标准
1. **对接 `dsh-permission-rules`**：接管它的 `ask` 档——把"卡住"变成"托管"（补位而非竞争）
2. **认证插件集（GovDistro 落地）**：签名 + 冲突测试 + 持续重认证（CI for compliance），服务政企采购
3. **标准草案**：把 escrow 语义写成可移植规范（对标 CCS/IETF draft 路线），让语义本身成为被抄的标准
4. **企业形态**：托管服务 + 签字意见书 + 保险精算数据（事故时间线四门生意的"定价"层）

### 兼容策略（不重复造轮子）

| 领域 | 已有方案 | 我们的策略 |
|---|---|---|
| 配置/插件树回滚 | dsh-undo-savepoint | 直接用 |
| 文件编辑回滚 | dsh-rollback | 直接用，v0.2 补命令层 |
| 权限规则（allow/deny） | dsh-permission-rules | v1.0 对接其 ask 档 |
| 运行时安全扫描 | dsh-plugin-ccs | 互补（它是静态+运行时扫描，我们是执行期托管） |
| **生效前托管（第四态）** | **无人做** | **本插件独占** |

## 十一、与「公开轨迹即信用」的关系

本插件 MIT 开源、代码原创（干净室实现，五个 Inpsired by 已在 README 致谢）、全部决策写入 append-only 账本——**账本本身就是第一份公开轨迹**。它既是产品，也是"单人认证者"路径的起点：一个能证明自己行为的插件，比一百页 PPT 更能建立信任。
